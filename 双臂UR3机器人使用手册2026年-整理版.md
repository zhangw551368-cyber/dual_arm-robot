# 双臂 UR3 机器人使用手册（整理版）

> 适用工作区：`/home/xiaoai/gzu_ws`  
> 整理依据：当前工作区文件 + 原《双臂UR3机器人使用手册-课题组成员之前写的.docx》  
> 核查日期：2026-05-26

## 1. 使用前确认

### 1.1 安全要求

1. 首次启动、改动 IP、改动 MoveIt 配置或运行新程序前，必须确认机械臂工作空间内无人、无障碍物。
2. 示教器急停、保护停止解除、机械臂上电、抱闸释放等动作，以现场负责人的安全流程为准。
3. 用 RViz/MoveIt 执行真实机械臂轨迹前，先点击 `Plan`，确认规划轨迹合理，再执行 `Execute`。
4. 夹爪调试时不要把手放在夹爪内，先用低夹持力、小行程测试。

### 1.2 网络规划

| 设备 | IP 地址 | 备注 |
| --- | --- | --- |
| 控制 PC | `192.168.1.41` | 当前手册按此地址写启动说明 |
| 左臂 UR3 | `192.168.1.43` | `ur3_dual_bringup.launch` 已写死 |
| 右臂 UR3 | `192.168.1.44` | `ur3_dual_bringup.launch` 已写死 |
| UR16e | `192.168.1.102` | 可选设备 |
| Robotiq 3F gripper | `192.168.1.11` | TCP 控制时使用 |
| 路由器 | `192.168.1.1` | 旧手册记录管理密码：`admin123` |

注意事项：

1. 所有设备接路由器 LAN 口，不接 WAN 口。
2. 控制 PC、两台 UR3、UR16e 应在 `192.168.1.x` 同一网段。
3. 如果控制 PC IP 改变，示教器 External Control 程序里的 Host IP 也要同步改。
4. 当前双臂驱动文件中端口分配如下：
   - 左臂：`40001`、`40002`、`40003`、`40004`
   - 右臂：`50001`、`50002`、`50003`、`50004`

### 1.3 终端环境

每开一个新终端都先执行：

```bash
conda deactivate
source /home/xiaoai/gzu_ws/devel/setup.bash
```

如果是在旧账号或旧电脑上操作，原手册中的路径可能是：

```bash
source /home/gzu/gzu_ws/devel/setup.bash
```

本机当前实际路径是 `/home/xiaoai/gzu_ws`。

## 2. 工作区文件地图

当前工作区是 ROS catkin 工作区，主要目录如下：

| 路径 | 作用 |
| --- | --- |
| `src/ur_robot_driver` | UR 真实机械臂驱动，包含 UR3/UR16e bringup、控制器配置 |
| `src/ur_description` | UR 机器人模型、URDF/Xacro、Gazebo 加载文件 |
| `src/ur3_dual_moveit_config` | 双 UR3 MoveIt 配置，包含 SRDF、规划、RViz、仿真启动 |
| `src/ur16e_moveit_config` | UR16e MoveIt 配置 |
| `src/robotiq` | Robotiq 2F/3F 夹爪、力传感器、Modbus RTU/TCP 控制 |
| `src/moveit_calibration` | MoveIt 手眼标定插件 |
| `build/`、`devel/` | catkin 编译产物，通常不用手动改 |

重点文件：

| 文件 | 说明 |
| --- | --- |
| `src/ur_robot_driver/launch/ur3_dual_bringup.launch` | 双臂真实机器人驱动入口 |
| `src/ur_robot_driver/config/left_arm_ur3_calibration.yaml` | 左臂标定参数 |
| `src/ur_robot_driver/config/right_arm_ur3_calibration.yaml` | 右臂标定参数 |
| `src/ur_robot_driver/config/ur3_controllers_left.yaml` | 左臂 ros_control 控制器 |
| `src/ur_robot_driver/config/ur3_controllers_right.yaml` | 右臂 ros_control 控制器 |
| `src/ur3_dual_moveit_config/launch/move_group.launch` | 双臂 MoveIt move_group |
| `src/ur3_dual_moveit_config/launch/moveit_rviz.launch` | 双臂 MoveIt RViz |
| `src/ur3_dual_moveit_config/config/ur3_dual_controllers_moveit.yaml` | MoveIt 到左右臂轨迹控制器的映射 |
| `src/robotiq/robotiq_2f_gripper_control/nodes/Robotiq2FGripperRtuNode_dual.py` | 双 2F 夹爪 RTU 控制节点 |

## 3. 双臂 UR3 真实机器人启动

### 3.1 启动前检查

在控制 PC 上检查网络：

```bash
ping 192.168.1.43
ping 192.168.1.44
```

能 ping 通再继续。若 ping 不通，先检查网线、路由器 LAN 口、IP 地址、示教器网络设置。

在两台 UR3 示教器上确认：

1. 机器人已上电并解除抱闸。
2. External Control URCap/程序已配置，Host IP 指向控制 PC，例如 `192.168.1.41`。
3. 准备运行 External Control 程序。驱动启动后，如果终端等待机器人连接，需要在示教器上按播放。

### 3.2 启动双臂驱动

终端 1：

```bash
conda deactivate
source /home/xiaoai/gzu_ws/devel/setup.bash
roslaunch ur_robot_driver ur3_dual_bringup.launch
```

这个 launch 会做几件事：

1. 加载双臂 URDF：`ur_description/launch/load_ur3_dual.launch`
2. 启动左右臂各自的 `ur_hardware_interface`
3. 聚合 `/left_arm/joint_states` 与 `/right_arm/joint_states`
4. 启动 `robot_state_publisher`
5. 启动左右臂 `scaled_pos_joint_traj_controller`

当前固定 IP：

| 机械臂 | 命名空间 | IP | TF 前缀 |
| --- | --- | --- | --- |
| 左臂 | `/left_arm` | `192.168.1.43` | `left_arm_` |
| 右臂 | `/right_arm` | `192.168.1.44` | `right_arm_` |

如果机械臂 IP 改了，需要改 `src/ur_robot_driver/launch/ur3_dual_bringup.launch` 中对应的 `robot_ip`。

### 3.3 启动 MoveIt

终端 2：

```bash
conda deactivate
source /home/xiaoai/gzu_ws/devel/setup.bash
roslaunch ur3_dual_moveit_config move_group.launch
```

`move_group.launch` 默认使用 `simple` controller manager，会读取：

```text
src/ur3_dual_moveit_config/config/ur3_dual_controllers_moveit.yaml
```

其中控制器映射为：

| MoveIt 控制器 | ROS action |
| --- | --- |
| `left_arm/scaled_pos_joint_traj_controller` | `/left_arm/scaled_pos_joint_traj_controller/follow_joint_trajectory` |
| `right_arm/scaled_pos_joint_traj_controller` | `/right_arm/scaled_pos_joint_traj_controller/follow_joint_trajectory` |

### 3.4 打开 RViz

终端 3：

```bash
conda deactivate
source /home/xiaoai/gzu_ws/devel/setup.bash
roslaunch ur3_dual_moveit_config moveit_rviz.launch rviz_config:=$(rospack find ur3_dual_moveit_config)/launch/moveit.rviz
```

在 RViz 的 MotionPlanning 面板中：

1. 选择 planning group，例如 `left_arm`、`right_arm` 或配置中已有的双臂 group。
2. 拖动目标位姿或设置目标关节值。
3. 先点 `Plan`，确认轨迹不会碰撞。
4. 再点 `Execute`，执行真实机械臂运动。

## 4. 双臂 UR3 仿真

只做 MoveIt 假执行，不连接真实机械臂：

```bash
conda deactivate
source /home/xiaoai/gzu_ws/devel/setup.bash
roslaunch ur3_dual_moveit_config demo.launch
```

启动 Gazebo + MoveIt：

```bash
conda deactivate
source /home/xiaoai/gzu_ws/devel/setup.bash
roslaunch ur3_dual_moveit_config demo_gazebo.launch
```

底层 Gazebo 启动文件还包括：

```bash
roslaunch ur_robot_driver ur3_dual_gazebo.launch
```

优先建议用 `ur3_dual_moveit_config demo_gazebo.launch`，因为它会同时启动 Gazebo 和 MoveIt/RViz 相关配置。

## 5. Robotiq 2F 夹爪

### 5.1 查看串口

```bash
ls /dev/ttyUSB*
```

如果没有设备，检查 USB 转串口线、电源和 udev 权限。

临时开放串口权限：

```bash
sudo chmod 666 /dev/ttyUSB0
sudo chmod 666 /dev/ttyUSB1
```

### 5.2 单夹爪启动

以一个夹爪在 `/dev/ttyUSB0` 为例：

```bash
conda deactivate
source /home/xiaoai/gzu_ws/devel/setup.bash
rosrun robotiq_2f_gripper_control Robotiq2FGripperRtuNode.py /dev/ttyUSB0
```

单节点默认话题通常为：

```text
/Robotiq2FGripperRobotInput
/Robotiq2FGripperRobotOutput
```

### 5.3 双夹爪启动

```bash
conda deactivate
source /home/xiaoai/gzu_ws/devel/setup.bash
rosrun robotiq_2f_gripper_control Robotiq2FGripperRtuNode_dual.py /dev/ttyUSB0 /dev/ttyUSB1
```

当前双夹爪节点使用的话题：

```text
/left_arm/Robotiq2FGripperRobotInput
/left_arm/Robotiq2FGripperRobotOutput
/right_arm/Robotiq2FGripperRobotInput
/right_arm/Robotiq2FGripperRobotOutput
```

### 5.4 常用控制命令

激活并张开左夹爪：

```bash
rostopic pub -1 /left_arm/Robotiq2FGripperRobotOutput robotiq_2f_gripper_control/Robotiq2FGripper_robot_output "rACT: 1
rGTO: 1
rATR: 0
rPR: 0
rSP: 255
rFR: 150"
```

激活并闭合左夹爪：

```bash
rostopic pub -1 /left_arm/Robotiq2FGripperRobotOutput robotiq_2f_gripper_control/Robotiq2FGripper_robot_output "rACT: 1
rGTO: 1
rATR: 0
rPR: 255
rSP: 255
rFR: 150"
```

激活并张开右夹爪：

```bash
rostopic pub -1 /right_arm/Robotiq2FGripperRobotOutput robotiq_2f_gripper_control/Robotiq2FGripper_robot_output "rACT: 1
rGTO: 1
rATR: 0
rPR: 0
rSP: 255
rFR: 150"
```

激活并闭合右夹爪：

```bash
rostopic pub -1 /right_arm/Robotiq2FGripperRobotOutput robotiq_2f_gripper_control/Robotiq2FGripper_robot_output "rACT: 1
rGTO: 1
rATR: 0
rPR: 255
rSP: 255
rFR: 150"
```

参数含义：

| 参数 | 含义 | 常用值 |
| --- | --- | --- |
| `rACT` | 激活夹爪 | `1` 激活，`0` 复位 |
| `rGTO` | 允许动作 | `1` 允许移动 |
| `rATR` | 自动释放 | 一般为 `0` |
| `rPR` | 目标位置 | `0` 全开，`255` 全闭 |
| `rSP` | 速度 | `0` 到 `255` |
| `rFR` | 力 | `0` 到 `255` |

夹爪异常时可先复位：

```bash
rostopic pub -1 /left_arm/Robotiq2FGripperRobotOutput robotiq_2f_gripper_control/Robotiq2FGripper_robot_output "rACT: 0"
```

然后重新发送 `rACT: 1` 的激活命令。

## 6. UR16e 可选启动

UR16e 驱动入口：

```bash
conda deactivate
source /home/xiaoai/gzu_ws/devel/setup.bash
roslaunch ur_robot_driver ur16e_bringup.launch
```

MoveIt：

```bash
roslaunch ur16e_moveit_config move_group.launch
roslaunch ur16e_moveit_config moveit_rviz.launch rviz_config:=$(rospack find ur16e_moveit_config)/launch/moveit.rviz
```

注意：当前 `ur16e_bringup.launch` 默认标定文件写为：

```text
$(find ur_description)/config/ur16e/my_ur16e_calibration.yaml
```

但当前工作区只看到 `default_kinematics.yaml`、`joint_limits.yaml`、`physical_parameters.yaml`、`visual_parameters.yaml`，未看到 `my_ur16e_calibration.yaml`。如果启动时报标定文件不存在，需要补充该文件或改 launch 默认参数。

## 7. Robotiq 3F gripper 可选启动

TCP 连接 3F 夹爪：

```bash
conda deactivate
source /home/xiaoai/gzu_ws/devel/setup.bash
rosrun robotiq_3f_gripper_control Robotiq3FGripperTcpNode.py 192.168.1.11
```

简单控制器：

```bash
rosrun robotiq_3f_gripper_control Robotiq3FGripperSimpleController.py
```

如果使用 RTU：

```bash
rosrun robotiq_3f_gripper_control Robotiq3FGripperRtuNode.py /dev/ttyUSB0
```

## 8. 视觉与手眼标定

原手册提到以下命令：

```bash
roslaunch kinect2_bridge kinect2_1.launch
roslaunch realsense2_camera rs_camera.launch
rosrun rqt_image_view rqt_image_view
python ur_vision.py
python ur3_moveit_pick.py
```

当前 `/home/xiaoai/gzu_ws` 中未找到 `ur_vision.py`、`ur3_moveit_pick.py`、`kinect2_bridge`、`realsense2_camera` 相关源码包。因此这些内容应视为外部包或旧电脑残留脚本，使用前需要确认：

1. 相机驱动是否已安装在系统 ROS 环境中。
2. 视觉脚本实际保存路径。
3. 脚本依赖的 Python 环境、OpenCV 版本和 ROS topic 名称。
4. 相机坐标系与机器人基坐标系之间是否已经完成手眼标定。

当前工作区包含 MoveIt 手眼标定相关包：

```text
src/moveit_calibration/moveit_calibration_gui
src/moveit_calibration/moveit_calibration_plugins
```

可在 RViz 中加载 MoveIt Calibration 插件进行手眼标定。具体标定流程建议单独整理成《视觉与手眼标定手册》，不要混在机器人基本启动手册里。

## 9. 常见问题

### 9.1 ping 不通机械臂

检查顺序：

1. 网线是否接入 LAN 口。
2. 控制 PC 是否在 `192.168.1.x` 网段。
3. 示教器中机械臂 IP 是否为 `192.168.1.43` / `192.168.1.44`。
4. 是否有 IP 冲突。
5. 路由器是否正常工作。

### 9.2 驱动启动了，但机械臂不能执行

检查顺序：

1. 示教器 External Control 程序是否正在运行。
2. External Control 的 Host IP 是否为控制 PC IP。
3. 终端是否出现类似 `Robot ready to receive control commands.` 的信息。
4. 控制器是否启动：

```bash
rosservice call /left_arm/controller_manager/list_controllers
rosservice call /right_arm/controller_manager/list_controllers
```

应看到 `scaled_pos_joint_traj_controller` 为 `running`。

### 9.3 RViz 中模型不动

检查：

```bash
rostopic echo /joint_states
rostopic echo /left_arm/joint_states
rostopic echo /right_arm/joint_states
```

如果左右臂有 joint_states，但 `/joint_states` 没有，检查 `ur3_dual_bringup.launch` 中的 `joint_state_publisher` 聚合节点是否正常。

### 9.4 MoveIt 规划成功但执行失败

检查：

1. `move_group.launch` 是否在驱动之后启动。
2. `src/ur3_dual_moveit_config/config/ur3_dual_controllers_moveit.yaml` 中控制器名称是否和运行中的 controller 一致。
3. `/left_arm/scaled_pos_joint_traj_controller/follow_joint_trajectory` 和 `/right_arm/scaled_pos_joint_traj_controller/follow_joint_trajectory` action 是否存在。

可检查 action：

```bash
rostopic list | grep follow_joint_trajectory
```

### 9.5 夹爪无响应

检查顺序：

1. `ls /dev/ttyUSB*` 是否能看到设备。
2. 串口权限是否足够。
3. 左右夹爪串口顺序是否接反。
4. 是否使用了正确话题：双夹爪节点是 `/left_arm/...` 和 `/right_arm/...`。
5. 先发送 `rACT: 0` 复位，再发送 `rACT: 1` 激活。

### 9.6 Qt 或 RViz 异常

如果运行 RViz/MoveIt 时出现 Qt 库冲突，旧手册建议检查 `~/.bashrc` 中的 `LD_LIBRARY_PATH`。处理方式：

1. 临时开一个干净终端。
2. 执行 `conda deactivate`。
3. 注释 `~/.bashrc` 中会覆盖系统 Qt/ROS Qt 的 `LD_LIBRARY_PATH` 配置。
4. 重新打开终端后再 source 工作区。

## 10. 推荐日常启动顺序

真实双臂 + MoveIt + RViz：

```bash
# 终端 1：双臂驱动
conda deactivate
source /home/xiaoai/gzu_ws/devel/setup.bash
roslaunch ur_robot_driver ur3_dual_bringup.launch
```

```bash
# 终端 2：MoveIt
conda deactivate
source /home/xiaoai/gzu_ws/devel/setup.bash
roslaunch ur3_dual_moveit_config move_group.launch
```

```bash
# 终端 3：RViz
conda deactivate
source /home/xiaoai/gzu_ws/devel/setup.bash
roslaunch ur3_dual_moveit_config moveit_rviz.launch rviz_config:=$(rospack find ur3_dual_moveit_config)/launch/moveit.rviz
```

双 2F 夹爪：

```bash
# 终端 4：双夹爪
conda deactivate
source /home/xiaoai/gzu_ws/devel/setup.bash
rosrun robotiq_2f_gripper_control Robotiq2FGripperRtuNode_dual.py /dev/ttyUSB0 /dev/ttyUSB1
```

仿真练习：

```bash
conda deactivate
source /home/xiaoai/gzu_ws/devel/setup.bash
roslaunch ur3_dual_moveit_config demo.launch
```

Gazebo 练习：

```bash
conda deactivate
source /home/xiaoai/gzu_ws/devel/setup.bash
roslaunch ur3_dual_moveit_config demo_gazebo.launch
```

## 11. 后续建议

1. 把机器人 IP、PC IP、夹爪串口号写成现场贴纸，避免每次猜。
2. 为 `/dev/ttyUSB0`、`/dev/ttyUSB1` 写 udev 规则，固定左右夹爪设备名，例如 `/dev/robotiq_left`、`/dev/robotiq_right`。
3. 把视觉识别、手眼标定、抓取程序单独整理成第二本文档。
4. 如果 UR16e 要长期使用，补齐 `my_ur16e_calibration.yaml` 或修正 `ur16e_bringup.launch` 默认标定路径。
5. 若后续有人改 IP 或 launch 文件，应同步更新本手册第一章和第三章。
