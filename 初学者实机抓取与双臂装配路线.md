# 初学者实机抓取与双臂装配路线

> 目标：在当前 `/home/xiaoai/gzu_ws` 基础上，先完成安全、可重复的实机控制，再完成一个简单抓取任务，最后逐步过渡到双臂机器人装配。  
> 建议原则：先单臂，后双臂；先关节空间，后笛卡尔空间；先固定位置抓取，后视觉抓取；先离线规划，后实机执行。

## 1. 总体路线

建议按下面 6 个阶段推进：

| 阶段 | 目标 | 完成标志 |
| --- | --- | --- |
| 阶段 0 | 熟悉安全、网络、启动流程 | 能稳定启动双 UR3 驱动和 RViz |
| 阶段 1 | 单臂实机运动 | 能用 MoveIt 控制左臂或右臂移动到指定姿态 |
| 阶段 2 | 夹爪控制 | 能独立控制 Robotiq 2F 夹爪张开、闭合、复位 |
| 阶段 3 | 固定位置抓取 | 能从固定桌面位置抓起一个物体并放到固定位置 |
| 阶段 4 | 双臂协同基础 | 能让左右臂按顺序运动，避免互相干涉 |
| 阶段 5 | 面向装配任务 | 能完成“抓取零件-移动-插入/放置/对准”的简化装配流程 |

不要一开始就做视觉抓取或双臂协作。对初学者来说，最容易卡住的不是算法，而是网络、坐标系、控制器、规划失败、实机安全这些基础问题。

## 2. 阶段 0：环境与安全确认

### 2.1 每次开机前检查

1. 确认机械臂周围无人。
2. 确认急停按钮可用。
3. 确认桌面没有可能碰撞的物体。
4. 确认网线接在路由器 LAN 口。
5. 确认控制 PC 与机械臂在同一网段。

当前推荐网络：

| 设备 | IP |
| --- | --- |
| 控制 PC | `192.168.1.41` |
| 左臂 UR3 | `192.168.1.43` |
| 右臂 UR3 | `192.168.1.44` |

检查网络：

```bash
ping 192.168.1.43
ping 192.168.1.44
```

如果 ping 不通，不要继续启动驱动，先解决网络。

### 2.2 终端环境

每个新终端都先执行：

```bash
cd /home/xiaoai/gzu_ws
command -v conda >/dev/null 2>&1 && conda deactivate || true
source /opt/ros/noetic/setup.bash
source /home/xiaoai/gzu_ws/devel/setup.bash
```

确认 ROS 能找到包：

```bash
rospack find ur_robot_driver
rospack find ur3_dual_moveit_config
rospack find robotiq_2f_gripper_control
```

### 2.3 先做仿真熟悉界面

在真实机械臂运动前，先用 MoveIt 假执行：

```bash
roslaunch ur3_dual_moveit_config demo.launch
```

你要熟悉：

1. RViz 中 MotionPlanning 面板在哪里。
2. 如何选择 planning group。
3. 如何拖动目标姿态。
4. 如何点击 `Plan`。
5. 如何查看规划轨迹。
6. 如何区分 `Plan` 预览和 `Execute` 真正执行。

这一阶段不要追求复杂任务，只要能理解 RViz 里的当前机器人、目标机器人、规划轨迹分别是什么。

## 3. 阶段 1：单臂实机运动

### 3.1 启动双臂驱动

终端 1：

```bash
command -v conda >/dev/null 2>&1 && conda deactivate || true
source /home/xiaoai/gzu_ws/devel/setup.bash
roslaunch ur_robot_driver ur3_dual_bringup.launch
```

然后在两台 UR3 示教器上运行 External Control 程序。终端中应能看到机器人连接成功、控制器启动等信息。

检查 joint states：

```bash
rostopic echo /joint_states
```

如果机械臂在示教器上被手动移动，RViz 中模型也应该跟着动。

### 3.2 启动 MoveIt

终端 2：

```bash
command -v conda >/dev/null 2>&1 && conda deactivate || true
source /home/xiaoai/gzu_ws/devel/setup.bash
roslaunch ur3_dual_moveit_config move_group.launch
```

### 3.3 启动 RViz

终端 3：

```bash
command -v conda >/dev/null 2>&1 && conda deactivate || true
source /home/xiaoai/gzu_ws/devel/setup.bash
roslaunch ur3_dual_moveit_config moveit_rviz.launch rviz_config:=$(rospack find ur3_dual_moveit_config)/launch/moveit.rviz
```

### 3.4 只控制一个机械臂

建议先只练左臂或右臂，不要一开始就动双臂。

在 RViz MotionPlanning 中：

1. Planning Group 选择 `left_arm` 或 `right_arm`。
2. 拖动末端目标，让机械臂只移动一点点。
3. 点击 `Plan`。
4. 确认轨迹没有碰撞、没有大幅绕转。
5. 把示教器速度倍率调低，例如 10% 到 20%。
6. 点击 `Execute`。

只点 `Plan` 时机器人不会动；它只是让 RViz 显示规划出的目标机器人和轨迹。确认轨迹安全后再点 `Execute`，真实机械臂才会收到控制器目标。

初学阶段建议只做这些动作：

1. 从当前位置移动到轻微抬高的位置。
2. 从当前位置移动到桌面上方安全高度。
3. 从一个桌面上方点移动到另一个桌面上方点。

不要直接让机械臂贴近桌面、夹具或另一台机械臂。

### 3.5 检查控制器状态

如果 MoveIt 执行失败，检查：

```bash
rosservice call /left_arm/controller_manager/list_controllers
rosservice call /right_arm/controller_manager/list_controllers
```

应看到 `scaled_pos_joint_traj_controller` 是 `running`。

也可以检查 action：

```bash
rostopic list | grep follow_joint_trajectory
```

应该能看到：

```text
/left_arm/scaled_pos_joint_traj_controller/follow_joint_trajectory/...
/right_arm/scaled_pos_joint_traj_controller/follow_joint_trajectory/...
```

## 4. 阶段 2：夹爪控制

### 4.1 查看串口

```bash
ls /dev/ttyUSB*
```

临时开放权限：

```bash
sudo chmod 666 /dev/ttyUSB0
sudo chmod 666 /dev/ttyUSB1
```

### 4.2 启动双夹爪节点

终端 4：

```bash
command -v conda >/dev/null 2>&1 && conda deactivate || true
source /home/xiaoai/gzu_ws/devel/setup.bash
rosrun robotiq_2f_gripper_control Robotiq2FGripperRtuNode_dual.py /dev/ttyUSB0 /dev/ttyUSB1
```

注意：`/dev/ttyUSB0` 和 `/dev/ttyUSB1` 的左右顺序可能会变。第一次测试时要观察哪个夹爪在动。

### 4.3 测试左夹爪

左夹爪张开：

```bash
rostopic pub -1 /left_arm/Robotiq2FGripperRobotOutput robotiq_2f_gripper_control/Robotiq2FGripper_robot_output "rACT: 1
rGTO: 1
rATR: 0
rPR: 0
rSP: 100
rFR: 80"
```

左夹爪闭合：

```bash
rostopic pub -1 /left_arm/Robotiq2FGripperRobotOutput robotiq_2f_gripper_control/Robotiq2FGripper_robot_output "rACT: 1
rGTO: 1
rATR: 0
rPR: 180
rSP: 100
rFR: 80"
```

初学阶段不要直接用 `rPR: 255`、`rFR: 255`，先用中等行程和较小夹持力。

### 4.4 测试右夹爪

右夹爪张开：

```bash
rostopic pub -1 /right_arm/Robotiq2FGripperRobotOutput robotiq_2f_gripper_control/Robotiq2FGripper_robot_output "rACT: 1
rGTO: 1
rATR: 0
rPR: 0
rSP: 100
rFR: 80"
```

右夹爪闭合：

```bash
rostopic pub -1 /right_arm/Robotiq2FGripperRobotOutput robotiq_2f_gripper_control/Robotiq2FGripper_robot_output "rACT: 1
rGTO: 1
rATR: 0
rPR: 180
rSP: 100
rFR: 80"
```

### 4.5 建议记录

建立一个实验记录表：

| 项目 | 记录 |
| --- | --- |
| `/dev/ttyUSB0` 对应 | 左夹爪 / 右夹爪 |
| `/dev/ttyUSB1` 对应 | 左夹爪 / 右夹爪 |
| 能夹住测试物体的 `rPR` | 例如 150 |
| 合适夹持力 `rFR` | 例如 80 |
| 合适速度 `rSP` | 例如 100 |

这会直接帮助后面的抓取任务。

## 5. 阶段 3：固定位置抓取

这是你最适合做的第一个完整任务。

任务定义：

1. 物体放在桌面固定位置。
2. 机械臂移动到物体上方。
3. 机械臂下降。
4. 夹爪闭合。
5. 机械臂抬起。
6. 移动到放置点上方。
7. 机械臂下降。
8. 夹爪张开。
9. 机械臂抬起回安全位。

### 5.1 先不用视觉

初学阶段不要一上来就做相机识别。固定位置抓取可以先把物体放在一个标记点，例如桌面贴一张胶带。

你需要先确定 4 个关键位姿：

| 名称 | 说明 |
| --- | --- |
| `home_pose` | 安全初始位 |
| `pre_grasp_pose` | 物体正上方，离桌面较高 |
| `grasp_pose` | 物体夹取高度 |
| `place_pose` | 放置位置上方或放置高度 |

### 5.2 用 RViz 手动找位姿

推荐先用 RViz 找这些点：

1. Planning Group 选择单臂，例如 `left_arm`。
2. 手动拖动末端到物体上方。
3. Plan 成功后执行。
4. 用下面命令记录 TF 或末端姿态。

查看 TF：

```bash
rosrun tf tf_echo world left_arm_tool0
```

如果 `world` 或 `left_arm_tool0` 不存在，用下面命令查看 TF 树中的真实 frame 名称：

```bash
rosrun tf view_frames
```

也可以先只记录关节角，不写笛卡尔位姿。

### 5.3 初学者更推荐关节角示教

固定位置抓取的第一版，建议使用关节角目标，而不是笛卡尔目标。

原因：

1. 关节角目标更稳定。
2. 不容易遇到逆解失败。
3. 适合先把完整流程跑通。
4. 之后再替换为笛卡尔位姿或视觉位姿。

你可以在 RViz 中把机械臂移动到关键位置，然后记录当前 `/joint_states`。

查看关节状态：

```bash
rostopic echo /joint_states
```

记录左臂 6 个关节：

```text
left_arm_shoulder_pan_joint
left_arm_shoulder_lift_joint
left_arm_elbow_joint
left_arm_wrist_1_joint
left_arm_wrist_2_joint
left_arm_wrist_3_joint
```

记录右臂 6 个关节：

```text
right_arm_shoulder_pan_joint
right_arm_shoulder_lift_joint
right_arm_elbow_joint
right_arm_wrist_1_joint
right_arm_wrist_2_joint
right_arm_wrist_3_joint
```

### 5.4 第一版抓取程序建议

建议新建一个自己的包，例如：

```bash
cd /home/xiaoai/gzu_ws/src
catkin_create_pkg gzu_dual_arm_tasks rospy moveit_commander geometry_msgs sensor_msgs std_msgs
mkdir -p gzu_dual_arm_tasks/scripts
cd /home/xiaoai/gzu_ws
catkin_make
source devel/setup.bash
```

建议脚本名称：

```text
src/gzu_dual_arm_tasks/scripts/left_arm_fixed_pick.py
```

程序逻辑：

1. 初始化 `moveit_commander`。
2. 连接 `left_arm` move group。
3. 发布夹爪张开命令。
4. 移动到 `home_joint_goal`。
5. 移动到 `pre_grasp_joint_goal`。
6. 移动到 `grasp_joint_goal`。
7. 发布夹爪闭合命令。
8. 移动回 `pre_grasp_joint_goal`。
9. 移动到 `pre_place_joint_goal`。
10. 移动到 `place_joint_goal`。
11. 发布夹爪张开命令。
12. 移动回 `home_joint_goal`。

### 5.5 第一次实机抓取的物体选择

建议物体：

1. 轻。
2. 软。
3. 大。
4. 形状规则。
5. 不怕摔。

比如泡沫块、海绵块、纸盒。不要一开始抓金属件、螺丝、细长件或易碎件。

### 5.6 成功标准

第一版不要要求很准。满足下面条件就算成功：

1. 机械臂能按顺序移动。
2. 夹爪能在正确时刻开合。
3. 能把物体从 A 点移动到 B 点。
4. 整个流程能重复 3 次以上。

## 6. 阶段 4：双臂协同基础

完成单臂固定抓取后，再做双臂。

### 6.1 第一个双臂任务

不要一开始做两臂同时运动。先做顺序动作：

1. 左臂从 A 点抓物体。
2. 左臂放到中间交接区。
3. 左臂退回安全位。
4. 右臂到交接区抓物体。
5. 右臂放到 B 点。
6. 右臂退回安全位。

这个任务能训练双臂装配需要的核心概念：

1. 左右臂工作空间划分。
2. 交接区设计。
3. 避碰。
4. 顺序控制。
5. 任务状态机。

### 6.2 建议设置三个区域

| 区域 | 用途 |
| --- | --- |
| 左臂工作区 | 左臂取料 |
| 中间交接区 | 左右臂交接或共同操作 |
| 右臂工作区 | 右臂放置或装配 |

双臂协作不是一开始就同时动，而是先把空间和时序规划清楚。

### 6.3 双臂程序结构

建议写成状态机：

```text
INIT
LEFT_OPEN_GRIPPER
LEFT_MOVE_PRE_GRASP
LEFT_MOVE_GRASP
LEFT_CLOSE_GRIPPER
LEFT_LIFT
LEFT_MOVE_HANDOVER
LEFT_OPEN_GRIPPER
LEFT_RETREAT
RIGHT_MOVE_HANDOVER
RIGHT_CLOSE_GRIPPER
RIGHT_LIFT
RIGHT_MOVE_PLACE
RIGHT_OPEN_GRIPPER
RIGHT_RETREAT
DONE
```

初学阶段用顺序执行就够了，不需要马上引入复杂框架。

## 7. 阶段 5：面向双臂装配

你的最终目标是双臂机器人装配，所以后续建议围绕“装配任务的最小版本”推进。

### 7.1 选择一个简单装配任务

建议从下面任务选一个：

1. 插圆柱到孔中。
2. 把积木放入固定槽。
3. 把一个零件放到另一个零件上。
4. 一只手固定底座，另一只手放置零件。
5. 左臂递零件，右臂装配。

最推荐第一个正式装配练习：

```text
左臂抓取圆柱零件 -> 放到装配区 -> 右臂抓取圆柱 -> 插入固定孔
```

孔可以先做得很大，允许误差大一些。

### 7.2 装配比抓取多了什么

装配任务需要额外解决：

1. 零件位姿更准。
2. 末端姿态更重要。
3. 接触力会影响任务。
4. 插入过程需要低速。
5. 失败后要能退出重试。

初期可以不用力控，先用“大间隙孔 + 低速插入 + 保守位姿”实现。

### 7.3 装配任务推荐分解

以插孔为例：

| 子任务 | 说明 |
| --- | --- |
| 取件 | 从固定位置抓起圆柱 |
| 移动 | 移到孔上方 |
| 对准 | 末端姿态垂直孔轴线 |
| 预插入 | 移到孔口上方 2 到 5 cm |
| 慢速插入 | 沿 z 方向缓慢下降 |
| 松开 | 夹爪打开 |
| 撤离 | 机械臂抬起回安全位 |

## 8. 推荐学习顺序

### 8.1 ROS 基础

你需要掌握：

1. `roslaunch`
2. `rosrun`
3. `rostopic list`
4. `rostopic echo`
5. `rostopic pub`
6. `rosservice call`
7. `rosnode list`
8. `tf_echo`

不需要一开始学完整 ROS，只要围绕调试机械臂够用即可。

### 8.2 MoveIt 基础

你需要掌握：

1. planning group 是什么。
2. joint target 和 pose target 的区别。
3. `Plan` 和 `Execute` 的区别。
4. 为什么会规划失败。
5. 为什么真实机器人当前状态必须和 MoveIt 当前状态一致。

### 8.3 机械臂基础

你需要掌握：

1. 关节空间。
2. 笛卡尔空间。
3. 末端执行器坐标系。
4. 基坐标系。
5. 奇异位形。
6. 工作空间限制。

### 8.4 双臂基础

你需要掌握：

1. 左右臂命名空间。
2. 左右臂 TF 前缀。
3. 双臂避碰。
4. 双臂任务时序。
5. 交接区设计。

## 9. 你下一步最该做什么

建议你接下来按这个顺序实际操作：

### 第 1 天：只跑通启动

1. 启动 `ur3_dual_bringup.launch`。
2. 启动 `move_group.launch`。
3. 启动 `moveit_rviz.launch`。
4. 在 RViz 中确认两台 UR3 模型和实机状态一致。
5. 不执行复杂动作。

### 第 2 天：单臂小幅运动

1. 只选择左臂。
2. 规划一个很小的移动。
3. 示教器速度倍率调低。
4. 执行。
5. 记录成功和失败信息。

### 第 3 天：夹爪独立控制

1. 查看 `/dev/ttyUSB*`。
2. 启动双夹爪节点。
3. 分别测试左夹爪和右夹爪。
4. 记录左右串口对应关系。
5. 记录适合测试物体的 `rPR`、`rSP`、`rFR`。

### 第 4 到 5 天：固定位置单臂抓取

1. 在桌面贴一个取物点和放置点。
2. 用 RViz 找几个安全关节位姿。
3. 记录关节角。
4. 写 `left_arm_fixed_pick.py`。
5. 先空跑，不放物体。
6. 再放软物体测试。

### 第 6 到 7 天：右臂重复同样流程

把左臂抓取流程复制到右臂，确认右臂也能单独完成固定位置抓取。

### 第 8 天以后：双臂顺序协作

先做左臂抓取、放到中间区、右臂再抓取的顺序任务。稳定后再考虑双臂同时运动。

## 10. 不建议现在立刻做的事情

这些事情可以以后做，但不建议你现在一开始就做：

1. 视觉识别抓取。
2. 双臂同时抓同一个物体。
3. 高精度插孔。
4. 力控装配。
5. 强化学习或复杂规划算法。
6. 自己从零写轨迹控制器。

原因很简单：当前最重要的是先获得一个稳定、可重复、你能理解的实机闭环。

## 11. 最小可行成果

你当前阶段最值得完成的第一个成果是：

```text
单臂 UR3 + Robotiq 2F 夹爪，从固定 A 点抓起海绵块，移动到固定 B 点并放下。
```

这个成果虽然简单，但它已经包含真实机器人任务的完整链路：

1. ROS 启动。
2. 机器人驱动。
3. MoveIt 规划。
4. 实机执行。
5. 夹爪控制。
6. 顺序任务逻辑。
7. 安全调试。

完成这个以后，再做双臂装配就有地基了。
